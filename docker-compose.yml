services:
  frontend:
    build: ./frontend
    ports:
      - "${CANDLR_PORT:-8080}:80"
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    build: ./backend
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    expose:
      - "8000"
    restart: unless-stopped
